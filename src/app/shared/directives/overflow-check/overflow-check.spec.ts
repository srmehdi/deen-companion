import { OverflowCheck } from './overflow-check';

describe('OverflowCheck', () => {
  it('should create an instance', () => {
    const directive = new OverflowCheck();
    expect(directive).toBeTruthy();
  });
});
